'use strict';

/**
 * @ngdoc function
 * @name dbaseEduApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the dbaseEduApp
 */
angular.module('dbaseEduApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
